const screens               = require('./screens');
const widgets               = require('./widgets');
const scenarios             = require('./scenarios');
const sessions              = require('./sessions');
const settings              = require('./settings');
const users                 = require('./users');
const bridge_types          = require('./bridge_types');
const simple_scenario_types = require('./simple_scenario_types');
const logs                  = require('./logs');
const notification_channels = require('./notification_channels');
const cities                = require('./cities');
const extensions            = require('./extensions');
const scenario_templates    = require('./scenario_templates');
const changelogs            = require('./changelogs');

module.exports = {
    screens,
    widgets,
    scenarios,
    sessions,
    settings,
    users,
    bridge_types,
    simple_scenario_types,
    logs,
    notification_channels,
    cities,
    extensions,
    scenario_templates,
    changelogs
};
